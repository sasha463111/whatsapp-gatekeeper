node_modules/
gatekeeper.db
gatekeeper.db-wal
gatekeeper.db-shm
.env
# Plaintext phone list — PII, never commit. Only approved-hashes.json is committed.
approved-phones.json
# Local helper, not needed in deployment
parse-csv.js
# WhatsApp Web session (contains auth tokens — never commit)
.wwebjs_auth/
.wwebjs_cache/
